# 闲鱼笔记（直接复制发帖）

闲鱼详情不认 Markdown。下面「标题」和「正文」按纯文本写，复制粘贴即可。  
主图用仓库里现成的图，顺序已经排好：封面先看懂卖什么，后面证明代码真能跑。

发帖时建议选类目：**技能服务 / 教育培训 / 编程辅导**（以 App 实际类目为准）。  
定位是 **带着把代码跑通、把图看懂**，不要写成代写论文。

---

## 标题（选一条）

1. Python深度学习辅导 图像分类VGG/ResNet代码已跑通  
2. 深度学习课设辅导 叶片病害分类 从训练到出图  
3. PyTorch图像分类辅导 VGG ResNet MobileNet都能跑

搜索里能搜到的词已经塞进标题：**Python、深度学习、图像分类、VGG、ResNet、课设、跑通**。  
一般显示前 30 个字，第 1 条最稳。

---

## 主图顺序（闲鱼封面很关键）

就发两张也够：一张正在跑，一张已经跑通。

1. `run_shots/shot_running.png`　**代码运行中**（真实终端，训到第 2 个 epoch）  
2. `run_shots/shot_success.png`　**代码跑通之后**（5/5 完成，绿字 PIPELINE OK，exit 0）

若闲鱼允许 9 张，后面再补：

3. `run_shots/11_success_illustration.png`　跑通海报  
4. `run_shots/08_success_final.png`　成功卡  
5. `run_shots/04_loss_acc_live.png`　Loss / Acc  
6. `run_shots/05_confusion_live.png`　混淆矩阵  
7. `figures/04_architecture_comparison.png`　四个主干对照  
8. `figures/C1_training_3x2.png`　论文风组图  
9. `run_shots/01_sample_leaves.png`　8 类样本  

封面不要用纯代码墙。就两张时：先 `shot_running`，再 `shot_success`。

---

## 正文（完整版，复制下面这一块）

```
Python深度学习辅导｜图像分类｜代码已在CPU上跑通

不是扔一堆网盘文件就不管。
这套是农作物叶片病害图像分类，8类，PyTorch能从头训到出测试结果。

【这次实跑，不是P的图】
设备：普通CPU（没有GPU也能看懂流程）
模型：TinyCNN 冒烟跑通整条流水线
5个epoch，没报错
验证准确率 92.86%
测试准确率 90.62%
Macro F1 90.55%
权重和指标都写盘了，exit code = 0

【课设/毕设真正要用的主干都在】
VGG-16
ResNet-50
MobileNetV2
EfficientNet-B0
提出模型：ResNet-50 + CBAM注意力

数据预处理、模型搭建、训练、验证、测试、早停，一条龙。
优化也能讲：AdamW、cosine学习率、Mixup/CutMix、label smoothing。

【图按论文习惯出】
Loss曲线、Accuracy曲线
优化器对比、学习率策略
结构对比（就是标题里那四个网络）
消融、数据增强
混淆矩阵、逐类P/R/F1、ROC
精度–参数量、t-SNE、训练流程图

英文坐标轴、白底、能直接放报告里。
不是检测的mAP，也不是分割的Dice，对的就是分类这条线。

【适合谁】
深度学习课设 / 图像分类小论文
只会调包、训练曲线看不懂的
有自己的叶片/皮肤/零件图片，想换成自己的数据接着训
电脑只有CPU，想先把流程跑通再上GPU

【怎么教】
一对一，按你的进度
先把demo跑绿，再换你的数据集和模型
图怎么画、指标怎么写进报告，对着你的结果讲
你自己交作业、自己写文字，我不代写论文

有自己的数据和截止日期，直接说，我按你的题来。
```

---

## 正文（短版，格子少或怕人嫌长时用）

```
Python深度学习辅导，图像分类，代码已跑通。

农作物叶片病害 8 类分类。
CPU上5个epoch：验证92.86%，测试90.62%，没报错。

能带着做：
数据预处理
VGG / ResNet / MobileNet / EfficientNet
训练测试、混淆矩阵、论文风训练图
ResNet+CBAM 也能讲

适合课设、自学、自己换数据集。
一对一讲解，不代写。有截止日期直接说。
```

---

## 服务说明（可放在正文末尾，按你实际情况改）

价格、时长、是否远程，**按你自己的报价填**，不要把仓库里的示例数当成卖点乱加零。建议只写你真正能交付的：

```
【交付】
可运行源码 + 训练/测试脚本
本次跑通截图和论文风训练图
远程屏幕共享，把环境装上、把命令跑绿
课后把你卡住的那一步讲清楚

【说明】
demo用合成小数据是为了几分钟内证明流水线通
正式做你的题目，换你的图、你的类别数、你选的主干
99.9%那种假完美准确率我不做，报告里经不起问
```

---

## 常见问法（评论区直接回）

还在吗  
→ 在，直接说课设还是自学、有没有自己的数据集、什么时候交。

能代做吗 / 能写论文吗  
→ 代码和实验可以带着跑通、图可以出，文字和答辩你自己来，不代写。

只有CPU行不行  
→ 行。我这边就是CPU跑通的，先把流程跑绿，有GPU再换大模型。

准确率能到多少  
→ demo冒烟是90%出头；换你的数据和ResNet/EfficientNet，按你的集重新训，不报假数。

和网上买的代码有什么区别  
→ 区别是能对着你的屏幕把报错修掉，并把Loss、混淆矩阵、消融讲到你能写进报告。

---

## 发帖前自检

- 标题里有「深度学习」和「图像分类」，不要只写「辅导」
- 封面是跑通海报，不是黑乎乎的 IDE
- 正文前三行就能看懂：能跑、是分类、有准确率
- 不要承诺代写、包过、包发表
- 终端截图用 `run_shots/03`、`07`，那是这次真实日志；`10_code_running.png` 是配图，数字不要当实测念
- `figures/` 里 96.81% 是完整实验设定的提出模型，和 TinyCNN demo 的 90.62% 不要写在同一句里抢功
